float y = 24.5 * 9
float x = 24.5 * 7.6 
float di = -1.4 / 6.0
float d = 1.4
float cut_width = 24.5 * 0.048

set stock size x y d

add layer
load "alliance_sf.dxf" 
clean

select all
	resize xy x y
	align x 0 
	align y 0 
	align z 0 
select none

select paths 9 10
select paths 12 14
select paths 16 18
	flip
select none

select path 3
loop 3 times
	offset z di
	mill offset paths 1.6
	offset z di
	mill offset paths 1.22
end loop
select none

loop i from 4 to 9
	select path i
	loop 3 times
		offset z di
		mill offset paths 1.75
		offset z di
		mill offset paths 1.22
	end loop
	select none
end loop

select path 10
loop 3 times
	offset z di
	mill offset paths 1.22
end loop
select none

loop i from 11 to 18
	select path i
	loop 3 times
		offset z di
		mill offset paths 1.75
		offset z di
		mill offset paths 1.22
	end loop
	select none
end loop

export gcode "deadspace.nc"